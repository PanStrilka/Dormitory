# 📌 Що треба від Арсена (памʼятка, щоб нічого не загубилось)

> Цей файл — «джерело правди» про те, що **чекає на тебе**, поки решту роблю я.
> Оновлюй галочки, коли щось зробиш. Гілка розробки: `claude/dorm-chores-system-szgx4o`.
> Дата останнього оновлення: 2026-09-02.

---

## 🔴 Крок 1 — Опублікувати сайт (2 хвилини, потрібен один клік)

GitHub не дозволяє зробити це за тебе, тому:

- [ ] Відкрий репозиторій → **Settings → Pages**
- [ ] **Build and deployment → Source:** обери **`GitHub Actions`** → збережи
- [ ] (за потреби) запусти вручну: вкладка **Actions → Deploy to GitHub Pages → Run workflow**
- [ ] Готово: сайт буде на **https://panstrilka.github.io/Dormitory/**

Після цього кожен мій пуш публікуватиметься автоматично.

---

## ✅ Крок 2 — Увімкнути спільну базу (Supabase) — Етап 1 — ГОТОВО

Ключі отримані й **вбудовані в застосунок** ([`js/config.js`](js/config.js)) —
синхронізація тепер вмикається автоматично для всіх (нікому не треба нічого
вставляти в Nastavení). Детально: [`docs/SETUP.md`](docs/SETUP.md), «Етап 1».

- [x] Створено проєкт на **<https://supabase.com>**
- [x] Виконано [`supabase/schema.sql`](supabase/schema.sql) у SQL Editor
- [x] Отримано **Project URL** + **publishable (anon)** ключ і вставлено в код
- [ ] **Перевір**: відкрий сайт → зелена крапка синхронізації вгорі = працює.
      Якщо крапка червона — переконайся, що `schema.sql` виконано повністю
      (у SQL Editor має бути `Success. No rows returned`).

> ⚠️ **Безпека:** у код пішов лише **публічний publishable-ключ** (він захищений
> RLS + кодом буньки). **Секретний** ключ (`sb_secret_…`) у репозиторій НЕ
> потрапив і не потрапить — цей сайт публічний на GitHub Pages. Оскільки ти
> скинув секретний ключ у чат, **рекомендую його про всяк випадок ротувати**
> (Supabase → Project Settings → API → Rotate). Застосунку він не потрібен —
> секрети живуть лише в Edge Functions → Secrets (Кроки 3–4).

---

## 🟡 Крок 3 — Чеки з ШІ (Етап 2) — коли скажеш, після Кроку 2

Детально: [`docs/SETUP.md`](docs/SETUP.md), «Етап 2».

- [ ] **Storage → New bucket** → назва `receipts` (Private)
- [ ] Заведи ключ ШІ на <https://console.anthropic.com> → **API Keys** *(секрет, мені не слати)*
- [ ] **Edge Functions → Secrets** → додай:
  - [ ] `ANTHROPIC_API_KEY` = твій ключ ШІ
  - [ ] *(рекомендовано для економії)* `ANTHROPIC_MODEL` = `claude-haiku-4-5`
- [ ] Розгорни функцію [`supabase/functions/parse-receipt/index.ts`](supabase/functions/parse-receipt/index.ts)
      (**Edge Functions → Deploy a new function**, назва `parse-receipt`)
- [ ] *(для перевірки фото банківського переказу)* розгорни також
      [`supabase/functions/verify-transfer/index.ts`](supabase/functions/verify-transfer/index.ts)
      (назва `verify-transfer`). Той самий ключ ШІ й bucket `receipts`.
      Без цієї функції погашення боргу переказом працює — фото просто
      зберігається як доказ, без автоперевірки ШІ.

---

## 🟡 Крок 4 — Пуш-сповіщення «твоя черга» (Етап 3) — коли скажеш

Детально: [`docs/SETUP.md`](docs/SETUP.md), «Етап 3».

- [ ] Згенеруй VAPID-ключі: <https://vapidkeys.com> або `npx web-push generate-vapid-keys`
- [ ] **Edge Functions → Secrets** → `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY` *(секрет)*, `VAPID_SUBJECT` = `mailto:твоя@пошта`
- [ ] Розгорни функцію [`supabase/functions/notify-duty/index.ts`](supabase/functions/notify-duty/index.ts) (назва `notify-duty`)
- [ ] У застосунку: **Nastavení → Oznámení** → встав **публічний** VAPID-ключ
- [ ] Постав розклад (cron) — SQL є в `docs/SETUP.md`, «Етап 3», крок 5

---

## ❓ Рішення/питання, на які колись треба відповісти (не термінові)

- [ ] **Мова навчалки/інтерфейсу** вже задана: чеська + англійська. Українську НЕ додаю (ти так сказав). Якщо передумаєш — скажи.
- [ ] **Модель ШІ для чеків:** за замовчуванням `claude-opus-5` (точніше), але дешевше — `claude-haiku-4-5`. Обери, коли робитимеш Крок 3.
- [ ] **Максимум мешканців** зараз 8. Якщо у вас інша кількість — скажи, зроблю гнучко.
- [ ] Чи потрібен **окремий пароль-адмін** (щоб не будь-хто міг схвалювати нових), чи вистачає коду буньки? Зараз схвалювати може будь-хто, хто зайшов. Скажи, якщо треба суворіше.

---

## ✅ Що вже готове (і запушене) — щоб ти був у курсі

- Дві рівні кімнати з редагованими назвами; прибрано «карму/повернення грошей».
- Навчалка з підказок (кнопка ❔), чек-листи, справедлива ротація, передача чергування.
- Витрати «хто кому винен» (Splitwise-стиль).
- Бали, рівні, жетони імунітету.
- **PWA** (встановлення на телефон + офлайн).
- **Верифікація** (код буньки + схвалення нових) — працює локально, засинхронізується з базою після Кроку 2.
- **Чеки з ШІ** — серверна функція + клієнт (активуються після Кроків 2–3).
- **Web-push** — клієнт + щотижнева функція розсилки (активується після Кроку 4).

---

### Як швидко відновити контекст зі мною
Просто напиши: «прочитай HANDOFF.md» — і я згадаю, де ми зупинились.
Найближча дія від тебе: **Крок 1 (Pages)** — увімкнути публікацію сайту.
Крок 2 (Supabase) вже підключено. Далі — за бажанням Кроки 3–4 (чеки з ШІ, пуші).
